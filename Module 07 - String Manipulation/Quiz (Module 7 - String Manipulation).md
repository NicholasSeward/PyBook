# Quiz (Module 7 - String Manipulation)

## Multiple Choice Questions (10 questions)

1. What is the ASCII code for the uppercase letter 'A'?
   a) 65
   b) 97
   c) 48
   d) 32

2. What is the output of `ord('a')`?
   a) 65
   b) 97
   c) 48
   d) 32

3. What is the output of `chr(65)`?
   a) 'A'
   b) 'a'
   c) '0'
   d) ' '

4. What is the purpose of the `\n` escape character?
   a) To create a tab
   b) To create a new line
   c) To escape quotes
   d) To create a backslash

5. What is the output of the following code?
   ```python
   text = "Hello"
   print(text[1])
   ```
   a) 'H'
   b) 'e'
   c) 'l'
   d) 'o'

6. What is the output of `"Hello".upper()`?
   a) `"hello"`
   b) `"HELLO"`
   c) `"Hello"`
   d) Error

7. What is the purpose of the `strip()` method?
   a) To remove whitespace from both ends
   b) To add whitespace to both ends
   c) To split a string into parts
   d) To join strings together

8. What is the output of `"hello world".split()`?
   a) `['hello', 'world']`
   b) `['h', 'e', 'l', 'l', 'o', ' ', 'w', 'o', 'r', 'l', 'd']`
   c) `'hello world'`
   d) Error

9. What is the purpose of the `join()` method?
   a) To split a string into parts
   b) To join a list of strings into one string
   c) To find a substring
   d) To replace characters

10. What is the output of `"Python".replace('P', 'J')`?
    a) `"Python"`
    b) `"Jython"`
    c) `"J"`
    d) Error

## Matching Questions (2 questions)

**Question 11:** Match each string method with its purpose.

| Method | Purpose |
|--------|---------|
| A) `upper()` | 1) Convert to lowercase |
| B) `lower()` | 2) Convert to uppercase |
| C) `title()` | 3) Capitalize first letter of each word |
| D) `capitalize()` | 4) Capitalize first letter only |

**Question 12:** Match each escape character with its meaning.

| Escape Character | Meaning |
|------------------|---------|
| A) `\n` | 1) Tab |
| B) `\t` | 2) New line |
| C) `\\` | 3) Backslash |
| D) `\"` | 4) Double quote |

## True/False Questions (3 questions)

13. Strings in Python are immutable. (True/False)

14. The `find()` method returns -1 if the substring is not found. (True/False)

15. Regular expressions can only be used with strings. (True/False)

## Fill in the Blank Questions (5 questions)

16. The `_____` function converts a character to its ASCII code.

17. The `_____` function converts an ASCII code to its character.

18. The `_____` method removes whitespace from both ends of a string.

19. The `_____` method splits a string into a list of substrings.

20. The `_____` method joins a list of strings into one string.
